{
  "name": "@factlayer/cli",
  "version": "0.6.0",
  "description": "Command-line tool for adding facts and re-verifying the ones that have gone stale.",
  "keywords": ["ai", "agent-memory", "llm", "freshness", "cli", "factlayer"],
  "type": "module",
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "factcheck": "./dist/cli.js"
  },
  "scripts": {
    "test": "bun test",
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "bun run ../../scripts/check-publish-ready.ts"
  },
  "dependencies": {
    "@factlayer/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
